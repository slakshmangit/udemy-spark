import org.apache.spark.SparkContext._
object POC {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(98); 
  val Names = parallelize(List("John", "Joe"));System.out.println("""Names  : <error> = """ + $show(Names ))}
}
